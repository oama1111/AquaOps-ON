[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers
markers =
    unit_blackbox: unit test driven from the module's public contract, without inspecting internals
    unit_whitebox: unit test that targets a named internal helper or a specific branch
