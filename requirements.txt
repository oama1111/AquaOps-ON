# Core (Unit 4+)
fastapi
uvicorn[standard]
sqlalchemy
pydantic
jinja2
PyYAML            # M4 rule catalogue (rules/oreg170.yaml)
python-multipart  # multipart/form-data for the laboratory CSV upload endpoint

# Algorithms (Unit 5)
pandas
numpy
scikit-learn
wntr

# Database (SQLite in dev; Postgres in deployment)
psycopg2-binary

# Testing (Unit 5/7)
pytest
pytest-cov
httpx  # FastAPI test client

# Tooling enforced by CI (.github/workflows/ci.yml)
ruff
mypy
